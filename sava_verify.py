# GENERATED — do not edit; run scripts/build_sava_verifier.py

"""Self-contained, stdlib-only reference verifier for sava-drop/1 Drops.

A cold agent -- given ONLY a Drop JSON file (and, optionally, its source
documents and a list of trusted public keys) and THIS ONE FILE -- can
verify the Drop with NOTHING installed: no pip, no pynacl, no pydantic.
Requires only the Python 3.10+ standard library.

This file is GENERATED by scripts/build_sava_verifier.py from the
engine's portable sava modules (acsa.sava._vendored.{jcs,ed25519_pure},
acsa.sava.{_atoms,canon,pae,ed25519_verify,rederive,source_fidelity,verify}).
Each module's source is embedded verbatim below and exec'd into its own
module namespace at import time, registered in sys.modules under its
original dotted name -- so the modules' own internal imports resolve
exactly as they do in the real package, and this file's behavior is, by
construction, IDENTICAL to acsa.sava.verify.verify_drop. Do not hand-edit
this file; re-run the build script instead.

CAUTION: do NOT `import` this file into a process that also uses the real
`acsa` package -- loading it registers fake `acsa.sava.*` entries in
`sys.modules`, which would shadow the real ones for the rest of that
process's lifetime. Run it only as `__main__` (`python3 sava_verify.py
...`); never `import sava_verify`.

Usage:
    python3 sava_verify.py <drop.json> [--sources <dir>] \
        [--trust <hex-pubkey-or-file>]... [--json]

  <drop.json>   Path to a Drop JSON file: {"content": {...}, "seal": {...}}.
  --sources     Optional directory of source documents, one file per
                source_id, named exactly as the source_id (e.g. a file
                named "ronin-excerpts" for source_id "ronin-excerpts").
                Used for byte-exact source-fidelity checking of included
                evidence quotes. Omit to skip source-fidelity checking
                entirely (this never by itself fails verification).
  --trust       A trusted Ed25519 public key: either a 64-character hex
                string, or a path to a file containing one 32-byte public
                key (as raw bytes, hex text, or base64 text). Repeatable;
                give one per trusted signer. Omit entirely to verify with
                an empty trust set (every signed Drop will then report
                result 3, "authenticity not established").
  --json        Print the raw {"result": ..., "source_fidelity": ...,
                "verdict": ..., "reasons": [...]} object as JSON instead
                of a human-readable summary.

Exit code == result["result"]:
  0 -- confirmed: content-address integrity, signer authenticity, and (for
       proof-carrying Drops) verdict re-derivation all check out.
  1 -- operational: a hard parse/format failure, a CLI usage problem (e.g.
       an unreadable file), or an unexpected internal error prevented any
       decision from being reached.
  2 -- failed: the Drop parsed and its signer is trusted, but something
       about the Drop itself is wrong (bad signature, verdict
       re-derivation mismatch, source fidelity mismatch, invalid
       composition, ...).
  3 -- authenticity not established: the seal's key_fingerprint matches no
       key in the supplied --trust set.
"""

import argparse
import base64
import contextlib
import datetime as dt
import io
import json
import sys
import types
from pathlib import Path

_MODULES = [
    ('acsa.sava._vendored.jcs', '# VENDORED rfc8785 v0.1.4 sha256=c25bc3a046528482d53bee3487b837f31dd9c05f33e8f13288c7aab320932cec - do not edit\n"""\nInternal implementation module for `rfc8785`.\n\nThis module is NOT a public API, and is not considered stable.\n"""\n\nfrom __future__ import annotations\n\nimport math\nimport re\nimport typing\nfrom io import BytesIO\n\n_Scalar = typing.Union[bool, int, str, float, None]\n\n_Value = typing.Union[\n    _Scalar,\n    typing.Sequence["_Value"],\n    typing.Tuple["_Value"],\n    typing.Mapping[str, "_Value"],\n]\n\n_INT_MAX = 2**53 - 1\n_INT_MIN = -(2**53) + 1\n\n# These are adapted from Andrew Rundgren\'s reference implementation,\n# which is licensed under the Apache License, version 2.0.\n# See: <https://github.com/cyberphone/json-canonicalization/blob/ba74d44ecf5/python3/src/org/webpki/json/Canonicalize.py>\n# See: <https://github.com/cyberphone/json-canonicalization/blob/ba74d44ecf5/python3/src/org/webpki/json/LICENSE>\n_ESCAPE = re.compile(r\'[\\x00-\\x1f\\\\"\\b\\f\\n\\r\\t]\')\n_ESCAPE_DCT = {\n    "\\\\": "\\\\\\\\",\n    \'"\': \'\\\\"\',\n    "\\b": "\\\\b",\n    "\\f": "\\\\f",\n    "\\n": "\\\\n",\n    "\\r": "\\\\r",\n    "\\t": "\\\\t",\n}\nfor i in range(0x20):\n    _ESCAPE_DCT.setdefault(chr(i), f"\\\\u{i:04x}")\n\n\nclass CanonicalizationError(ValueError):\n    """\n    The base error for all errors during canonicalization.\n    """\n\n    pass\n\n\nclass IntegerDomainError(CanonicalizationError):\n    """\n    The given integer exceeds the true integer precision of an\n    IEEE 754 double-precision float, which is what JSON uses.\n    """\n\n    def __init__(self, n: int) -> None:\n        """\n        Initialize an `IntegerDomainError`.\n        """\n        super().__init__(f"{n} exceeds safe integer domain for JSON floats")\n\n\nclass FloatDomainError(CanonicalizationError):\n    """\n    The given float cannot be represented in JCS, typically because it\'s\n    infinite, NaN, or an invalid representation.\n    """\n\n    def __init__(self, f: float) -> None:\n        """\n        Initialize an `FloatDomainError`.\n        """\n\n        super().__init__(f"{f} is not representable in JCS")\n\n\ndef _serialize_str(s: str, sink: typing.IO[bytes]) -> None:\n    """\n    Serialize a string as a JSON string, per RFC 8785 3.2.2.2.\n    """\n\n    def _replace(match: re.Match) -> str:\n        return _ESCAPE_DCT[match.group(0)]\n\n    sink.write(b\'"\')\n    try:\n        # Encoding to UTF-8 means that we\'ll reject surrogates and other\n        # non-UTF-8-isms.\n        sink.write(_ESCAPE.sub(_replace, s).encode("utf-8"))\n    except UnicodeEncodeError as e:\n        raise CanonicalizationError("input contains non-UTF-8 codepoints") from e\n    sink.write(b\'"\')\n\n\ndef _serialize_float(f: float, sink: typing.IO[bytes]) -> None:\n    """\n    Serialize a floating point number to a stable string format, as\n    defined in ECMA 262 7.1.12.1 and amended by RFC 8785 3.2.2.3.\n    """\n\n    # NaN and infinite forms are prohibited.\n    if math.isnan(f) or math.isinf(f):\n        raise FloatDomainError(f)\n\n    # Python does not distinguish between +0 and -0.\n    if f == 0:\n        sink.write(b"0")\n        return\n\n    # Negatives get serialized by prepending the sign marker and serializing\n    # the positive form.\n    if f < 0:\n        sink.write(b"-")\n        _serialize_float(-f, sink)\n        return\n\n    # The remainder of this implementation is adapted from\n    # Andrew Rundgren\'s reference implementation.\n\n    # Now we should only have valid non-zero values\n    stringified = str(f)\n\n    exponent_str = ""\n    exponent_value = 0\n    q = stringified.find("e")\n    if q > 0:\n        # Grab the exponent and remove it from the number\n        exponent_str = stringified[q:]\n        if exponent_str[2:3] == "0":\n            # Suppress leading zero on exponents\n            exponent_str = exponent_str[:2] + exponent_str[3:]\n        stringified = stringified[0:q]\n        exponent_value = int(exponent_str[1:])\n\n    # Split number in first + dot + last\n    first = stringified\n    dot = ""\n    last = ""\n    q = stringified.find(".")\n    if q > 0:\n        dot = "."\n        first = stringified[:q]\n        last = stringified[q + 1 :]\n\n    # Now the string is split into: first + dot + last + exponent_str\n    if last == "0":\n        # Always remove trailing .0\n        dot = ""\n        last = ""\n\n    if exponent_value > 0 and exponent_value < 21:\n        # Integers are shown as is with up to 21 digits\n        first += last\n        last = ""\n        dot = ""\n        exponent_str = ""\n        q = exponent_value - len(first)\n        while q >= 0:\n            q -= 1\n            first += "0"\n    elif exponent_value < 0 and exponent_value > -7:\n        # Small numbers are shown as 0.etc with e-6 as lower limit\n        last = first + last\n        first = "0"\n        dot = "."\n        exponent_str = ""\n        q = exponent_value\n        while q < -1:\n            q += 1\n            last = "0" + last\n\n    sink.write(f"{first}{dot}{last}{exponent_str}".encode())\n\n\ndef dumps(obj: _Value) -> bytes:\n    """\n    Perform JCS serialization of `obj`, returning the canonical serialization\n    as `bytes`.\n    """\n    # TODO: Optimize this?\n    sink = BytesIO()\n    dump(obj, sink)\n    return sink.getvalue()\n\n\ndef dump(obj: _Value, sink: typing.IO[bytes]) -> None:\n    """\n    Perform JCS serialization of `obj` into `sink`.\n    """\n\n    if obj is None:\n        sink.write(b"null")\n    elif isinstance(obj, bool):\n        obj = bool(obj)\n        if obj is True:\n            sink.write(b"true")\n        else:\n            sink.write(b"false")\n    elif isinstance(obj, int):\n        obj = int(obj)\n        if obj < _INT_MIN or obj > _INT_MAX:\n            raise IntegerDomainError(obj)\n        sink.write(str(obj).encode("utf-8"))\n    elif isinstance(obj, str):\n        # NOTE: We don\'t coerce with `str(...)`` here, since that will do\n        # the wrong thing for `(str, Enum)` subtypes where `__str__` is\n        # `Enum.__str__`.\n        _serialize_str(obj, sink)\n    elif isinstance(obj, float):\n        obj = float(obj)\n        _serialize_float(obj, sink)\n    elif isinstance(obj, (list, tuple)):\n        obj = list(obj)\n        if not obj:\n            # Optimization for empty lists.\n            sink.write(b"[]")\n            return\n\n        sink.write(b"[")\n        for idx, elem in enumerate(obj):\n            if idx > 0:\n                sink.write(b",")\n            dump(elem, sink)\n        sink.write(b"]")\n    elif isinstance(obj, dict):\n        obj = dict(obj)\n        if not obj:\n            # Optimization for empty dicts.\n            sink.write(b"{}")\n            return\n\n        # RFC 8785 3.2.3: Objects are sorted by key; keys are ordered\n        # by their UTF-16 encoding. The spec isn\'t clear about which endianness,\n        # but the examples imply that the big endian encoding is used.\n        try:\n            obj_sorted = sorted(obj.items(), key=lambda kv: kv[0].encode("utf-16be"))\n        except AttributeError:\n            # Failing to call `encode()` indicates that a key isn\'t a string.\n            raise CanonicalizationError("object keys must be strings")\n\n        sink.write(b"{")\n        for idx, (key, value) in enumerate(obj_sorted):\n            if idx > 0:\n                sink.write(b",")\n\n            _serialize_str(key, sink)\n            sink.write(b":")\n            dump(value, sink)\n\n        sink.write(b"}")\n    else:\n        raise CanonicalizationError(f"unsupported type: {type(obj)}")\n'),
    ('acsa.sava._vendored.ed25519_pure', '# VENDORED https://github.com/jamuir/EdDSA-rfc-python/blob/02551e232e757eb24bb0046b43a12794eb4426e9/Section-6.py sha256=1b9c03ee4e70466cbe8abef75db4a70144ab809e60e41d59a7bfdd8cac72dca4 - do not edit\n\'\'\'\n6.  Ed25519 Python Illustration\n\n   The rest of this section describes how Ed25519 can be implemented in\n   Python (version 3.2 or later) for illustration.  See Appendix A for\n   the complete implementation and Appendix B for a test-driver to run\n   it through some test vectors.\n\n   Note that this code is not intended for production as it is not\n   proven to be correct for all inputs, nor does it protect against\n   side-channel attacks.  The purpose is to illustrate the algorithm to\n   help implementers with their own implementation.\n\'\'\'\n\n## First, some preliminaries that will be needed.\n\nimport hashlib\n\ndef sha512(s):\n    return hashlib.sha512(s).digest()\n\n# Base field Z_p\np = 2**255 - 19\n\ndef modp_inv(x):\n    return pow(x, p-2, p)\n\n# Curve constant\nd = -121665 * modp_inv(121666) % p\n\n# Group order\nq = 2**252 + 27742317777372353535851937790883648493\n\ndef sha512_modq(s):\n    return int.from_bytes(sha512(s), "little") % q\n\n## Then follows functions to perform point operations.\n\n# Points are represented as tuples (X, Y, Z, T) of extended\n# coordinates, with x = X/Z, y = Y/Z, x*y = T/Z\n\ndef point_add(P, Q):\n    A, B = (P[1]-P[0]) * (Q[1]-Q[0]) % p, (P[1]+P[0]) * (Q[1]+Q[0]) % p;\n    C, D = 2 * P[3] * Q[3] * d % p, 2 * P[2] * Q[2] % p;\n    E, F, G, H = B-A, D-C, D+C, B+A;\n    return (E*F, G*H, F*G, E*H);\n\n# Computes Q = s * Q\ndef point_mul(s, P):\n    Q = (0, 1, 1, 0)  # Neutral element\n    while s > 0:\n        if s & 1:\n            Q = point_add(Q, P)\n        P = point_add(P, P)\n        s >>= 1\n    return Q\n\ndef point_equal(P, Q):\n    # x1 / z1 == x2 / z2  <==>  x1 * z2 == x2 * z1\n    if (P[0] * Q[2] - Q[0] * P[2]) % p != 0:\n        return False\n    if (P[1] * Q[2] - Q[1] * P[2]) % p != 0:\n        return False\n    return True\n\n## Now follows functions for point compression.\n\n# Square root of -1\nmodp_sqrt_m1 = pow(2, (p-1) // 4, p)\n\n# Compute corresponding x-coordinate, with low bit corresponding to\n# sign, or return None on failure\ndef recover_x(y, sign):\n    if y >= p:\n        return None\n    x2 = (y*y-1) * modp_inv(d*y*y+1)\n    if x2 == 0:\n        if sign:\n            return None\n        else:\n            return 0\n\n    # Compute square root of x2\n    x = pow(x2, (p+3) // 8, p)\n    if (x*x - x2) % p != 0:\n        x = x * modp_sqrt_m1 % p\n    if (x*x - x2) % p != 0:\n        return None\n\n    if (x & 1) != sign:\n        x = p - x\n    return x\n\n# Base point\ng_y = 4 * modp_inv(5) % p\ng_x = recover_x(g_y, 0)\nG = (g_x, g_y, 1, g_x * g_y % p)\n\ndef point_compress(P):\n    zinv = modp_inv(P[2])\n    x = P[0] * zinv % p\n    y = P[1] * zinv % p\n    return int.to_bytes(y | ((x & 1) << 255), 32, "little")\n\ndef point_decompress(s):\n    if len(s) != 32:\n        raise Exception("Invalid input length for decompression")\n    y = int.from_bytes(s, "little")\n    sign = y >> 255\n    y &= (1 << 255) - 1\n\n    x = recover_x(y, sign)\n    if x is None:\n        return None\n    else:\n        return (x, y, 1, x*y % p)\n\n## These are functions for manipulating the private key.\n\ndef secret_expand(secret):\n    if len(secret) != 32:\n        raise Exception("Bad size of private key")\n    h = sha512(secret)\n    a = int.from_bytes(h[:32], "little")\n    a &= (1 << 254) - 8\n    a |= (1 << 254)\n    return (a, h[32:])\n\ndef secret_to_public(secret):\n    (a, dummy) = secret_expand(secret)\n    return point_compress(point_mul(a, G))\n\n## The signature function works as below.\n\ndef sign(secret, msg):\n    a, prefix = secret_expand(secret)\n    A = point_compress(point_mul(a, G))\n    r = sha512_modq(prefix + msg)\n    R = point_mul(r, G)\n    Rs = point_compress(R)\n    h = sha512_modq(Rs + A + msg)\n    s = (r + h * a) % q\n    return Rs + int.to_bytes(s, 32, "little")\n\n## And finally the verification function.\n\ndef verify(public, msg, signature):\n    if len(public) != 32:\n        raise Exception("Bad public key length")\n    if len(signature) != 64:\n        raise Exception("Bad signature length")\n    A = point_decompress(public)\n    if not A:\n        return False\n    Rs = signature[:32]\n    R = point_decompress(Rs)\n    if not R:\n        return False\n    s = int.from_bytes(signature[32:], "little")\n    if s >= q: return False\n    h = sha512_modq(Rs + public + msg)\n    sB = point_mul(s, G)\n    hA = point_mul(h, A)\n    return point_equal(sB, point_add(R, hA))\n\n## Basic testing\n\nimport binascii\n\n# Section 7.1\n# -----TEST 1\n\nsk_hex = b\'\\\n9d61b19deffd5a60ba844af492ec2cc4\\\n4449c5697b326919703bac031cae7f60\\\n\'\n\npk_hex = b\'\\\nd75a980182b10ab7d54bfed3c964073a\\\n0ee172f3daa62325af021a68f707511a\\\n\'\n\nmsg = b\'\'\n\nsig_hex = b\'\\\ne5564300c360ac729086e2cc806e828a\\\n84877f1eb8e5d974d873e06522490155\\\n5fb8821590a33bacc61e39701cf9b46b\\\nd25bf5f0595bbe24655141438e7a100b\\\n\'\n\nsk = binascii.unhexlify(sk_hex)\npk = binascii.unhexlify(pk_hex)\nsig = binascii.unhexlify(sig_hex)\n\nassert secret_to_public(sk) == pk\nassert sign(sk, msg) == sig\nassert verify(pk, msg, sig)\n\nassert secret_to_public(sk[:31] + b\'\\x60\') == pk\nassert secret_to_public(sk[:31] + b\'0\') != pk\n\nassert sign(sk, msg + b\'0\') != sig\nassert sign(sk[:31] + b\'0\', msg) != sig\n\nassert verify(pk, msg + b\'0\', sig) == False\nassert verify(pk[:31] + b\'0\', msg, sig) == False\nassert verify(pk, msg, sig[:63] + b\'0\') == False\n\ntry:\n    verify(pk[:31], msg, sig)\n    raise Exception("This exception should not be raised")\nexcept Exception as E:\n    assert str(E) == "Bad public key length"\n\ntry:\n    verify(pk, msg, sig[:63])\n    raise Exception("This exception should not be raised")\nexcept Exception as E:\n    assert str(E) == "Bad signature length"\n\n\nprint("All tests passed.")\n'),
    ('acsa.sava._atoms', '# Portable copy of acsa/verify/structured.py atom logic — kept in sync via test_atoms_agree_with_engine\n"""Deterministic atom extraction + consistency (spec §5 step 5).\n\nCategories: money (unit=currency symbol), percent, date (unit="date",\nvalue=(year, month|0, day|0)), number (unit=""). Magnitude suffixes K/M/B and\nwords thousand/million/billion normalize the value. [Not established]: coverage\nbeyond these categories — anything else is not an atom and never blocks a verdict.\n\nThis is a portable copy for acsa.sava.rederive: stdlib-only, Python\n3.10-compatible, no pydantic / acsa.core.models / acsa.verify.* imports.\nIt exists so the re-derivation core can be lifted into a hash-pinned,\ndependency-free reference verifier (Task 10). Do not import the engine\'s\nacsa.verify.structured here -- that would defeat the point.\n"""\nimport re\n\n_MONTHS = {m.lower(): i for i, m in enumerate(\n    ["January","February","March","April","May","June","July","August",\n     "September","October","November","December"], 1)}\n_MONTHS.update({m[:3].lower(): i for m, i in list(_MONTHS.items())})\n_MAG = {"k": 1e3, "m": 1e6, "b": 1e9, "bn": 1e9,\n        "thousand": 1e3, "million": 1e6, "billion": 1e9}\n\n_MONEY = re.compile(r"([$€£])\\s?(\\d+(?:[.,]\\d+)*)\\s*(k|m|b|bn|thousand|million|billion)?\\b", re.I)\n_PERCENT = re.compile(r"(\\d+(?:[.,]\\d+)*)\\s?%")\n_ISO_DATE = re.compile(r"\\b(\\d{4})-(\\d{2})(?:-(\\d{2}))?\\b")\n_MONTH_DATE = re.compile(r"\\b(" + "|".join(_MONTHS) + r")\\.?\\s+(\\d{4})\\b", re.I)\n_NUMBER = re.compile(r"\\b(\\d+(?:[.,]\\d+)*)\\s*(k|m|b|bn|thousand|million|billion)?\\b", re.I)\n\ndef _num(s: str, mag: str | None) -> float:\n    v = float(s.replace(",", ""))\n    return v * _MAG[mag.lower()] if mag else v\n\ndef extract_atoms(text: str) -> set[tuple[str, str, object]]:\n    atoms: set[tuple[str, str, object]] = set()\n    spans: list[tuple[int, int]] = []\n    for m in _MONEY.finditer(text):\n        atoms.add(("money", m.group(1), _num(m.group(2), m.group(3))))\n        spans.append(m.span())\n    for m in _PERCENT.finditer(text):\n        atoms.add(("percent", "%", _num(m.group(1), None)))\n        spans.append(m.span())\n    for m in _ISO_DATE.finditer(text):\n        atoms.add(("date", "date", (int(m.group(1)), int(m.group(2)), int(m.group(3) or 0))))\n        spans.append(m.span())\n    for m in _MONTH_DATE.finditer(text):\n        atoms.add(("date", "date", (int(m.group(2)), _MONTHS[m.group(1).lower()[:3]], 0)))\n        spans.append(m.span())\n    for m in _NUMBER.finditer(text):\n        if any(s <= m.start() and m.end() <= e for s, e in spans):\n            continue  # already consumed by money/percent/date\n        atoms.add(("number", "", _num(m.group(1), m.group(2))))\n    return atoms\n\ndef atom_dates(text: str) -> set[tuple[int, int, int]]:\n    return {v for c, _, v in extract_atoms(text) if c == "date"}  # type: ignore[misc]\n\ndef check_consistency(claim_text: str, quotes: list[str]) -> tuple[str, str]:\n    """Returns (result, detail): result in {"consistent","contradiction","ambiguous"}.\n\n    Per spec §5.5: every claim atom must appear in at least one quote. Same\n    category+unit present in quotes with only different value(s) -> contradiction.\n    Category+unit absent from all quotes -> ambiguous. A claim with no atoms is\n    consistent (grounding alone decides).\n    """\n    claim_atoms = extract_atoms(claim_text)\n    if not claim_atoms:\n        return "consistent", "no atoms in claim"\n    quote_atoms: set[tuple[str, str, object]] = set()\n    for q in quotes:\n        quote_atoms |= extract_atoms(q)\n    for cat, unit, value in sorted(claim_atoms, key=repr):\n        same_cu = {v for c, u, v in quote_atoms if (c, u) == (cat, unit)}\n        if value in same_cu:\n            continue\n        if same_cu:\n            return "contradiction", f"claim {cat}{unit}={value!r}; quotes have {sorted(same_cu, key=repr)!r}"\n        return "ambiguous", f"claim atom {cat}{unit}={value!r} absent from quotes"\n    return "consistent", f"{len(claim_atoms)} atoms matched"\n'),
    ('acsa.sava.canon', '"""SAVA canonical JSON (RFC 8785 JCS) utilities.\n\nThis module provides canonicalization and strict parsing for RFC 8785 JSON\nCanonicalization Scheme (JCS). It is designed for broad runtime compatibility:\nrequires Python >=3.10, stdlib-only at module level. The canonicalize() and\nassert_idempotent() functions may be extracted verbatim into the pinned\nreference verifier.\n\nDesigned for broad runtime: requires Python >=3.10, stdlib-only at module level.\n"""\n\nimport hashlib\nimport json\nfrom decimal import Decimal\nfrom typing import Any\n\nfrom acsa.sava._vendored import jcs\n\n\ndef _check_no_floats_or_decimals(obj: Any, path: list[str] | None = None) -> None:\n    """Recursively check that obj contains no float or Decimal values.\n\n    Raises ValueError if any float or Decimal is found, including the path\n    to the offending value in the error message.\n\n    Args:\n        obj: The object to check.\n        path: List of keys/indices leading to obj (for error messages).\n    """\n    if path is None:\n        path = []\n\n    if isinstance(obj, (float, Decimal)):\n        path_str = "".join(f"[{p!r}]" for p in path) if path else "(root)"\n        raise ValueError(\n            f"canonicalize rejects float/Decimal values (SAVA number rule); "\n            f"found {type(obj).__name__} at {path_str}: {obj}"\n        )\n    elif isinstance(obj, dict):\n        for key, value in obj.items():\n            _check_no_floats_or_decimals(value, path + [key])\n    elif isinstance(obj, (list, tuple)):\n        for idx, item in enumerate(obj):\n            _check_no_floats_or_decimals(item, path + [idx])\n\n\ndef canonicalize(obj: dict) -> bytes:\n    """RFC 8785 JCS bytes of obj (delegates to the vendored jcs module).\n\n    Rejects float/Decimal values anywhere in obj (the SAVA number rule):\n    integers only, everything non-integer-numeric must be a string.\n\n    Args:\n        obj: A dictionary to canonicalize.\n\n    Returns:\n        bytes: RFC 8785 canonical JSON serialization.\n\n    Raises:\n        ValueError: If obj contains any float or Decimal values.\n    """\n    _check_no_floats_or_decimals(obj)\n    return jcs.dumps(obj)\n\n\ndef content_id(content: dict) -> str:\n    """Compute sha256 hex digest of canonicalized content.\n\n    Args:\n        content: A dictionary to hash.\n\n    Returns:\n        str: Lowercase hexadecimal sha256 digest of canonicalize(content).\n    """\n    canonical = canonicalize(content)\n    return hashlib.sha256(canonical).hexdigest()\n\n\ndef strict_decode(raw: bytes) -> Any:\n    """Parse raw as STRICT JSON: no canonical-form check, just the guards.\n\n    Performs strict parsing:\n    - Rejects duplicate keys (at any nesting level)\n    - Rejects NaN and Infinity constants\n    - Rejects trailing data (non-whitespace after valid JSON)\n\n    This is the shared strict-decode core used both by assert_idempotent\n    (which additionally requires canonicalize(parsed) == raw, a check\n    specific to re-verifying an already-canonicalized `content`\n    sub-object) and by acsa.sava.verify\'s Drop-envelope parsing (whose\n    wire bytes need not be pre-canonicalized -- ordinary json.dumps\n    output is a perfectly normal wire Drop). Keeping exactly one copy of\n    these guards avoids the two independently-written copies drifting\n    apart, which matters here: this is root-of-trust parsing code that\n    Task 10 lifts verbatim into the pinned reference verifier.\n\n    Args:\n        raw: UTF-8 encoded JSON bytes to parse.\n\n    Returns:\n        The parsed JSON value (a dict for any well-formed SAVA payload,\n        but any valid JSON value for malformed/adversarial input).\n\n    Raises:\n        ValueError: On invalid UTF-8, duplicate keys, NaN/Infinity, or\n                    trailing data.\n    """\n    # Helper to track duplicate keys\n    def pairs_hook(pairs):\n        seen = set()\n        for key, value in pairs:\n            if key in seen:\n                raise ValueError(f"Duplicate key in JSON: {key}")\n            seen.add(key)\n        return dict(pairs)\n\n    # Helper to reject NaN/Infinity\n    def parse_constant(s):\n        raise ValueError(\n            f"JSON constants (NaN, Infinity, -Infinity) are not allowed; "\n            f"found: {s}"\n        )\n\n    # Decode the raw bytes\n    try:\n        text = raw.decode(\'utf-8\')\n    except UnicodeDecodeError as e:\n        raise ValueError(f"Invalid UTF-8 in raw bytes: {e}") from e\n\n    # Parse with strict checks\n    decoder = json.JSONDecoder(\n        object_pairs_hook=pairs_hook,\n        parse_constant=parse_constant,\n    )\n    try:\n        parsed, idx = decoder.raw_decode(text)\n    except json.JSONDecodeError as e:\n        raise ValueError(f"Invalid JSON: {e}") from e\n\n    # Check for trailing data (non-whitespace after the decoded object)\n    remaining = text[idx:].strip()\n    if remaining:\n        raise ValueError(\n            f"Trailing data after valid JSON: {remaining[:50]}"\n        )\n\n    return parsed\n\n\ndef assert_idempotent(raw: bytes) -> dict:\n    """Parse raw as STRICT JSON and verify it canonicalizes to itself.\n\n    Delegates the strict-parse guards (duplicate keys, NaN/Infinity,\n    trailing data) to strict_decode, then additionally verifies\n    canonicalize(parsed) == raw (byte-for-byte).\n\n    Args:\n        raw: UTF-8 encoded JSON bytes to parse and verify.\n\n    Returns:\n        dict: The parsed JSON object.\n\n    Raises:\n        ValueError: On duplicate keys, NaN/Infinity, trailing data,\n                    or if canonicalize(parsed) != raw.\n    """\n    parsed = strict_decode(raw)\n\n    # Verify idempotence: canonicalize(parsed) must equal raw\n    canonical = canonicalize(parsed)\n    if canonical != raw:\n        raw_preview = raw[:50] if len(raw) > 50 else raw\n        canonical_preview = canonical[:50] if len(canonical) > 50 else canonical\n        raise ValueError(\n            f"JSON is not in canonical form (untrusted input does not match "\n            f"canonical output). Input (first 50 bytes): {raw_preview!r}; "\n            f"canonical form: {canonical_preview!r}"\n        )\n\n    return parsed\n'),
    ('acsa.sava.pae', '"""DSSE Pre-Authentication Encoding (PAE) for SAVA.\n\nThis module provides the PAE encoding used to bind a protocol context string\nto a Drop\'s content_id, preventing signature-lifting attacks.\n\nDesigned for broad runtime compatibility: requires Python >=3.10, stdlib-only\nat module level. The pae() and signing_input() functions can be extracted\nverbatim into the pinned reference verifier.\n"""\n\n\ndef pae(type_str: str, body: bytes) -> bytes:\n    """DSSE Pre-Authentication Encoding (secure-systems-lab DSSE spec).\n\n    Encodes type_str and body into a canonicalized format:\n    b"DSSEv1" + b" " + LEN(type) + b" " + type_utf8 + b" " + LEN(body) + b" " + body\n\n    where:\n    - LEN(x) is the ASCII-decimal byte length of x (e.g. b"11" for 11 bytes)\n    - type_utf8 is the UTF-8 encoding of type_str\n    - Separators are single ASCII spaces (0x20)\n\n    Args:\n        type_str: The protocol/type string (e.g. "sava-drop/1")\n        body: The raw bytes to encode (e.g. the 32-byte digest)\n\n    Returns:\n        bytes: The encoded PAE\n    """\n    type_bytes = type_str.encode("utf-8")\n    type_len = str(len(type_bytes)).encode("ascii")\n    body_len = str(len(body)).encode("ascii")\n\n    return b"DSSEv1 " + type_len + b" " + type_bytes + b" " + body_len + b" " + body\n\n\ndef signing_input(content_id_hex: str, type_str: str = "sava-drop/1") -> bytes:\n    """Compute the signing input for a SAVA object.\n\n    The signing input is the DSSE PAE of `type_str` bound to the\n    content_id digest. Defaults to "sava-drop/1" (unchanged from before\n    this param existed) so every existing caller keeps working verbatim;\n    new object families (sava-pond/1, sava-lake/1, sava-witness/1,\n    sava-key-rotation/1) pass their own type_str explicitly.\n\n    Args:\n        content_id_hex: A 64-character hex string (the sha256 hex digest).\n            The 32 raw bytes are extracted and used as the PAE body.\n        type_str: The protocol/type string to bind. Defaults to\n            "sava-drop/1".\n\n    Returns:\n        bytes: The DSSE PAE with the given type and the 32-byte digest.\n    """\n    content_bytes = bytes.fromhex(content_id_hex)\n    return pae(type_str, content_bytes)\n'),
    ('acsa.sava.ed25519_verify', '"""\nPure-Python Ed25519 signature verification.\n\nA stdlib-only, verification-only Ed25519 wrapper for verifying SAVA\nDrops. This module is a hardening layer around the vendored, byte-for-byte\nRFC 8032 §6 reference implementation\n(`acsa.sava._vendored.ed25519_pure`).\n\nArchitecture: the vendored file is kept verbatim (unmodified upstream\nsource, sha256-pinned — see\n`tests/test_sava_ed25519.py::test_vendored_ed25519_sha256_pinned`) so it\ncan be diffed against its upstream at any time. The reference `verify()`\nalready rejects non-canonical S (`if s >= q: return False`) and\nnon-canonical points A/R (via `point_decompress`), so it is not, on its\nown, unsafe. This wrapper adds explicit, plan-mandated DEFENSE-IN-DEPTH\naround that reference, so the security posture does not depend solely on\nreading and trusting third-party source:\n\n    1. Reject malformed input lengths, before any parsing.\n    2. Reject non-canonical S (the classic Ed25519 malleability vector,\n       S >= L where L is the group order) BEFORE any further processing —\n       redundant with the reference\'s own check, deliberately, so our own\n       code (not just the vendored copy) is the thing standing between a\n       malleable signature and acceptance.\n    3. Delegate to the vendored reference `verify`, which rejects\n       non-canonical point encodings of A and R (y-coordinate >= p) via\n       `point_decompress`, and computes the RFC 8032 §5.1.7 verification\n       equation.\n    4. Never raise: the vendored reference implementation raises on some\n       malformed inputs (e.g. bad lengths) rather than returning False;\n       this wrapper catches everything and returns False instead.\n\nThis module can be used by consumers with ZERO external dependencies at\nthe point of verification.\n"""\n\nfrom acsa.sava._vendored.ed25519_pure import q as _L\nfrom acsa.sava._vendored.ed25519_pure import verify as _verify_impl\n\n# _L is the group (subgroup) order, per RFC 8032 §5.1, imported directly\n# from the vendored reference (named `q` there) rather than re-typed as a\n# literal here — a future mismatch between our copy and the reference\'s\n# becomes an ImportError/AttributeError, not a silent divergence.\n#\n# A signature (R, S) is only canonical if S < L; S >= L (e.g. S\' = S + L)\n# verifies against the same equation as S, since S is only ever used mod L,\n# so accepting S >= L would make signatures malleable. This check MUST run\n# here, before delegating to the reference implementation, and MUST NOT be\n# reordered after it (see module docstring: defense-in-depth, not a gap-fill).\n\n\ndef verify(public32: bytes, message: bytes, signature64: bytes) -> bool:\n    """\n    Verify an Ed25519 signature.\n\n    Returns True iff signature64 is a valid, canonical Ed25519 signature of\n    message under public32. Returns False on ANY malformed input (wrong\n    lengths, non-canonical encodings) and on any non-canonical/malleable\n    signature. NEVER raises.\n\n    Args:\n        public32: 32-byte Ed25519 public key\n        message: message bytes\n        signature64: 64-byte Ed25519 signature\n\n    Returns:\n        bool: True iff the signature is valid, False otherwise (or on malformed input)\n\n    Security properties (enforced in this order):\n        - Rejects wrong-length public keys/signatures.\n        - Rejects non-canonical S (S >= L, the group order) — the\n          malleability guard.\n        - Rejects non-canonical encodings of public key A (y >= p).\n        - Rejects non-canonical encodings of point R (y >= p).\n        - Follows RFC 8032 §5.1.7 verification.\n        - Returns False (never raises) on any invalid input.\n    """\n    try:\n        if len(public32) != 32 or len(signature64) != 64:\n            return False\n\n        # Canonical-S check (malleability guard). CRITICAL: must happen\n        # before calling into the reference implementation.\n        s = int.from_bytes(signature64[32:], "little")\n        if s >= _L:\n            return False\n\n        return bool(_verify_impl(public32, message, signature64))\n    except Exception:\n        # Any unexpected exception (including the reference implementation\'s\n        # own raises on malformed input) is treated as verification failure.\n        return False\n'),
    ('acsa.sava.merkle', '"""SAVA Merkle module (spec Sec 3): sorted, domain-separated Merkle\ntree over member content_ids, with inclusion proofs, non-inclusion\nproofs, and an optional delta-based consistency check.\n\nStdlib-only (hashlib, bisect), Python 3.10-compatible: this module is\nlifted VERBATIM into the pinned reference verifier (see\nscripts/build_sava_verifier.py\'s MODULE_SPECS), so it must never import\nanything beyond the standard library.\n\nCONSTRUCTION (spec Sec 3.1): leaves are member content_ids, sorted\nlexicographically by hex string (equivalent to sorting by raw digest\nbytes, since sha256().hexdigest() is fixed-width lowercase). Leaves are\ndomain-separated from internal nodes (0x00 vs 0x01 prefix) -- the\nstandard hardening against second-preimage attacks (RFC 6962 / CT). An\nunpaired node at any level is PROMOTED UNCHANGED to the next level, never\nduplicated (duplicating the last node is the classic Merkle attack this\navoids). The empty-member tree\'s root is the fixed constant sha256(0x00).\n\nWHY THIS MODULE MATTERS (spec Sec 3.3): a bare pair of independent\ninclusion proofs for two members L and R does NOT prove nothing sits\nbetween them -- a withheld member Z with L < Z < R is fully consistent\nwith both proofs verifying. This module\'s non-inclusion construction\ncloses that gap by binding L and R to ADJACENT sorted indices\n(index(R) == index(L) + 1), derived from each proof\'s own path/\nmember_count, never trusted as a bare wire-supplied field. See\nTestHiddenMemberRejected in tests/test_sava_merkle.py for the\nadversarial proof this actually holds.\n"""\nimport bisect\nimport hashlib\n\n#: Fixed root for a tree with zero members (spec Sec 3.1).\nEMPTY_ROOT = hashlib.sha256(b"\\x00").hexdigest()\n\n\ndef leaf_hash(content_id_hex: str) -> bytes:\n    """sha256(0x00 || raw 32 bytes of content_id_hex) -- domain-separated\n    from node_hash so an internal node can never be replayed as a leaf."""\n    return hashlib.sha256(b"\\x00" + bytes.fromhex(content_id_hex)).digest()\n\n\ndef node_hash(left: bytes, right: bytes) -> bytes:\n    """sha256(0x01 || left || right) over two 32-byte child hashes."""\n    return hashlib.sha256(b"\\x01" + left + right).digest()\n\n\ndef _levels(leaf_hashes: list) -> list:\n    """Bottom-up level list: levels[0] = leaf hashes, levels[-1] = [root\n    hash] (a single-element list, even for a one-member tree). An\n    unpaired trailing node at any level is promoted UNCHANGED (never\n    duplicated) to the next level -- see module docstring."""\n    levels = [leaf_hashes]\n    cur = leaf_hashes\n    while len(cur) > 1:\n        nxt = []\n        i = 0\n        n = len(cur)\n        while i < n:\n            if i + 1 < n:\n                nxt.append(node_hash(cur[i], cur[i + 1]))\n                i += 2\n            else:\n                nxt.append(cur[i])  # promoted unchanged\n                i += 1\n        levels.append(nxt)\n        cur = nxt\n    return levels\n\n\ndef build_tree(content_ids: list) -> dict:\n    """Build a sorted Merkle tree over `content_ids` (a list of 64-hex\n    content_id strings; duplicates are not deduplicated by this function\n    -- callers must pass a set of distinct members).\n\n    Returns:\n        {"root": hex str, "levels": [[bytes, ...], ...], "sorted_ids": [str, ...]}\n        `levels`/`sorted_ids` are producer-side detail (used by\n        prove_inclusion/prove_non_inclusion below); a verifier only ever\n        needs `root`.\n    """\n    if not content_ids:\n        return {"root": EMPTY_ROOT, "levels": [], "sorted_ids": []}\n    sorted_ids = sorted(content_ids)\n    leaves = [leaf_hash(cid) for cid in sorted_ids]\n    levels = _levels(leaves)\n    root = levels[-1][0].hex()\n    return {"root": root, "levels": levels, "sorted_ids": sorted_ids}\n\n\ndef _level_sizes(member_count: int) -> list:\n    """n_0 = member_count, n_{i+1} = ceil(n_i / 2), down to n_k = 1 (spec\n    Sec 3.3) -- fixes every level\'s size deterministically from\n    member_count alone, independent of any proof."""\n    sizes = [member_count]\n    n = member_count\n    while n > 1:\n        n = -(-n // 2)  # ceil division, stdlib-only\n        sizes.append(n)\n    return sizes\n\n\ndef prove_inclusion(tree: dict, content_id_hex: str) -> dict:\n    """Build an inclusion proof for content_id_hex, which must already be\n    a member of `tree` (as returned by build_tree). Raises ValueError if\n    it is not a member -- this is a producer-side construction helper,\n    never called with untrusted input.\n\n    Returns: {"content_id", "path", "index", "member_count"} (spec Sec 3.3\n    wire format). `path` is leaf-to-root ordered.\n    """\n    sorted_ids = tree["sorted_ids"]\n    idx = sorted_ids.index(content_id_hex)\n    levels = tree["levels"]\n    path = []\n    cur_idx = idx\n    for level in levels[:-1]:\n        n = len(level)\n        if cur_idx % 2 == 0:\n            if cur_idx + 1 < n:\n                path.append({"side": "right", "hash": level[cur_idx + 1].hex()})\n            else:\n                path.append({"promoted": True})\n        else:\n            path.append({"side": "left", "hash": level[cur_idx - 1].hex()})\n        cur_idx //= 2\n    return {\n        "content_id": content_id_hex,\n        "path": path,\n        "index": idx,\n        "member_count": len(sorted_ids),\n    }\n\n\ndef derive_index(path: list, member_count: int):\n    """Derive a leaf\'s absolute sorted index purely from its proof\'s\n    `path` and the head\'s `member_count` (spec Sec 3.3) -- walking\n    root-to-leaf, the REVERSE of how `path` is built (leaf-to-root).\n    Returns None if the path is structurally inconsistent with\n    member_count (wrong length, a promoted step at a level where the\n    arithmetic doesn\'t work out). Never trusts a bare wire-supplied\n    index field -- this IS the independent recomputation.\n    """\n    if member_count is None or member_count <= 0:\n        return None\n    sizes = _level_sizes(member_count)\n    depth = len(sizes) - 1\n    if len(path) != depth:\n        return None\n    index = 0\n    for level_from_top, step in enumerate(reversed(path)):\n        level_index_from_leaf = depth - 1 - level_from_top\n        n_i = sizes[level_index_from_leaf]\n        if step.get("promoted"):\n            expected = n_i - 1\n            if 2 * index != expected:\n                return None\n            index = expected\n        else:\n            side = step.get("side")\n            if side == "left":\n                index = 2 * index + 1\n            elif side == "right":\n                index = 2 * index\n            else:\n                return None\n    return index\n\n\ndef verify_inclusion(proof: dict, root_hex: str) -> bool:\n    """Verify an inclusion proof against a head\'s merkle_root. Recomputes\n    BOTH the hash path (bottom-up, to the claimed root) AND the leaf\'s\n    index (from path/member_count, per derive_index) -- never trusts a\n    bare wire-supplied index. Never raises."""\n    try:\n        member_count = proof["member_count"]\n        path = proof["path"]\n        idx = derive_index(path, member_count)\n        if idx is None:\n            return False\n        if "index" in proof and proof["index"] != idx:\n            return False\n\n        cur = leaf_hash(proof["content_id"])\n        for step in path:\n            if step.get("promoted"):\n                continue\n            sib = bytes.fromhex(step["hash"])\n            side = step.get("side")\n            if side == "right":\n                cur = node_hash(cur, sib)\n            elif side == "left":\n                cur = node_hash(sib, cur)\n            else:\n                return False\n        return cur.hex() == root_hex\n    except Exception:\n        return False\n\n\ndef prove_non_inclusion(tree: dict, target_content_id_hex: str) -> dict:\n    """Build a non-inclusion proof for target_content_id_hex against\n    `tree` (spec Sec 3.3, all 4 forms). Raises ValueError if the target\n    IS actually a member -- this is a producer-side construction helper.\n    """\n    sorted_ids = tree["sorted_ids"]\n    member_count = len(sorted_ids)\n    if member_count == 0:\n        return {"target_content_id": target_content_id_hex, "form": "empty", "member_count": 0}\n\n    pos = bisect.bisect_left(sorted_ids, target_content_id_hex)\n    if pos < member_count and sorted_ids[pos] == target_content_id_hex:\n        raise ValueError("target_content_id is a member; cannot prove non-inclusion")\n\n    if pos == 0:\n        right = prove_inclusion(tree, sorted_ids[0])\n        return {\n            "target_content_id": target_content_id_hex,\n            "form": "left_boundary",\n            "right": right,\n            "member_count": member_count,\n        }\n    if pos == member_count:\n        left = prove_inclusion(tree, sorted_ids[-1])\n        return {\n            "target_content_id": target_content_id_hex,\n            "form": "right_boundary",\n            "left": left,\n            "member_count": member_count,\n        }\n    left = prove_inclusion(tree, sorted_ids[pos - 1])\n    right = prove_inclusion(tree, sorted_ids[pos])\n    return {\n        "target_content_id": target_content_id_hex,\n        "form": "neighbors",\n        "left": left,\n        "right": right,\n        "member_count": member_count,\n    }\n\n\ndef verify_non_inclusion(proof: dict, root_hex: str) -> bool:\n    """Verify a non-inclusion proof against a head\'s merkle_root. Never\n    raises. See module docstring: the adjacency check\n    (index(R) == index(L) + 1) is what makes this actually prove absence\n    -- not merely that L and R are each independently members."""\n    try:\n        form = proof.get("form")\n        target = proof["target_content_id"]\n        member_count = proof.get("member_count")\n\n        if form == "empty":\n            return member_count == 0 and root_hex == EMPTY_ROOT\n\n        if member_count is None or member_count <= 0:\n            return False\n\n        if form == "neighbors":\n            left, right = proof.get("left"), proof.get("right")\n            if left is None or right is None:\n                return False\n            if not (left["content_id"] < target < right["content_id"]):\n                return False\n            if not verify_inclusion(left, root_hex) or not verify_inclusion(right, root_hex):\n                return False\n            li = derive_index(left["path"], member_count)\n            ri = derive_index(right["path"], member_count)\n            if li is None or ri is None:\n                return False\n            return ri == li + 1\n\n        if form == "left_boundary":\n            right = proof.get("right")\n            if right is None:\n                return False\n            if not (target < right["content_id"]):\n                return False\n            if not verify_inclusion(right, root_hex):\n                return False\n            ri = derive_index(right["path"], member_count)\n            return ri == 0\n\n        if form == "right_boundary":\n            left = proof.get("left")\n            if left is None:\n                return False\n            if not (target > left["content_id"]):\n                return False\n            if not verify_inclusion(left, root_hex):\n                return False\n            li = derive_index(left["path"], member_count)\n            return li == member_count - 1\n\n        return False\n    except Exception:\n        return False\n\n\ndef verify_consistency(old_members: list, delta: dict, new_root_hex: str) -> bool:\n    """Apply `delta` ({"added": [...], "removed": [...]}) to old_members\n    and rebuild the tree locally, comparing the result to new_root_hex\n    (spec Sec 3.4). Genuinely O(n) -- there is no shortcut for a\n    sorted-by-value tree under middle insertions/removals. Never raises.\n    """\n    try:\n        members = set(old_members)\n        for cid in delta.get("removed", []):\n            members.discard(cid)\n        for cid in delta.get("added", []):\n            members.add(cid)\n        return build_tree(list(members))["root"] == new_root_hex\n    except Exception:\n        return False\n'),
    ('acsa.sava.head_verify', '"""SAVA collection-head verifier -- the cold, stateless receiver side\nof sava-pond/1 / sava-lake/1 (spec Sec 5.1, 12.2).\n\nverify_head is a SIBLING to acsa.sava.verify.verify_drop, not a call\ninto it: collection heads carry no derived_from/verdict, so there is\nstructurally no composition fold to run. This module also carries the\nwitness-quorum and rotation-signature checks (Sec 6, 7) -- all pure,\nstateless, no network I/O -- added in later tasks.\n\nPortability: this module is lifted VERBATIM into the portable,\nhash-pinned reference verifier (Task 5), so it MUST stay stdlib-only at\nmodule level and Python-3.10-compatible. It imports only its sava\nsiblings (canon, pae, ed25519_verify) -- never pydantic or acsa.crypto.\n\nThe key-fingerprint algorithm and ISO-8601 parsing helper are reproduced\nhere (not imported from acsa.sava.verify) for the same reason\nverify.py itself reproduces them rather than importing acsa.crypto: this\nmodule must stay independently portable, and (per MODULE_SPECS\ndependency order, Task 5) head_verify.py is embedded BEFORE verify.py,\nso it cannot import it even if it wanted to.\n\nRESULT CODES (mirrors verify_drop\'s tri-state+operational scheme):\n  0 -- confirmed: structure, content-address, signature, and freshness\n       all check out.\n  1 -- operational: malformed input, unknown format, or a missing\n       required field -- no decision could be reached.\n  2 -- failed: signature invalid, key_fingerprint mismatch between\n       content and seal, or the head has expired.\n  3 -- authenticity not established: seal\'s key_fingerprint matches no\n       key in trust_set.\n"""\nimport base64\nimport datetime as dt\nimport hashlib\n\nfrom acsa.sava import canon, ed25519_verify, pae\n\n_REQUIRED_CONTENT_FIELDS = (\n    "hash_alg", "domain", "key_fingerprint", "version", "expiry",\n    "prev_head", "merkle_root", "member_count",\n)\n\n\ndef _parse_iso(s: str) -> dt.datetime:\n    """Duplicated from acsa.sava.verify\'s identical helper, deliberately\n    -- see module docstring (independent portability + MODULE_SPECS\n    ordering means this module cannot import verify.py)."""\n    if s.endswith("Z"):\n        s = s[:-1] + "+00:00"\n    return dt.datetime.fromisoformat(s)\n\n\ndef _result(result: int, fmt, domain, version, reasons: list) -> dict:\n    return {"result": result, "format": fmt, "domain": domain, "version": version, "reasons": reasons}\n\n\ndef verify_head(raw: bytes, *, trust_set: set, now: dt.datetime) -> dict:\n    """Verify a sava-pond/1 or sava-lake/1 collection head (raw JSON\n    bytes). Never raises.\n\n    Args:\n        raw: raw wire bytes: {"content": {...}, "seal": {...}, "witnesses": [...], "rotation": ...|null}.\n        trust_set: set of raw 32-byte Ed25519 public keys the receiver trusts.\n        now: caller-supplied reference time for the expiry check.\n\n    Returns:\n        {"result": 0|1|2|3, "format": str|None, "domain": str|None,\n         "version": int|None, "reasons": [str, ...]}\n    """\n    try:\n        return _verify_head(raw, trust_set=trust_set, now=now)\n    except Exception as e:\n        return _result(1, None, None, None, [f"internal error: {e}"])\n\n\ndef _verify_head(raw: bytes, *, trust_set, now) -> dict:\n    try:\n        head = canon.strict_decode(raw)\n    except Exception as e:\n        return _result(1, None, None, None, [f"parse error: {e}"])\n\n    if not isinstance(head, dict) or "content" not in head or "seal" not in head:\n        return _result(1, None, None, None, ["malformed head: missing content or seal"])\n\n    content = head["content"]\n    seal = head["seal"]\n    if not isinstance(content, dict) or not isinstance(seal, dict):\n        return _result(1, None, None, None, ["malformed head: content/seal must be objects"])\n\n    fmt = content.get("format")\n    if fmt not in ("sava-pond/1", "sava-lake/1"):\n        return _result(1, fmt, content.get("domain"), content.get("version"),\n                        ["unknown or missing format"])\n\n    missing = [f for f in _REQUIRED_CONTENT_FIELDS if f not in content]\n    if missing:\n        return _result(1, fmt, content.get("domain"), content.get("version"),\n                        [f"missing required field(s): {missing}"])\n\n    domain = content["domain"]\n    version = content["version"]\n\n    if content["key_fingerprint"] != seal.get("key_fingerprint"):\n        return _result(2, fmt, domain, version,\n                        ["key_fingerprint mismatch between content and seal"])\n\n    cid = canon.content_id(content)\n    candidates = [\n        k for k in trust_set if hashlib.sha256(k).hexdigest()[:16] == seal.get("key_fingerprint")\n    ]\n    if not candidates:\n        return _result(3, fmt, domain, version, ["signer not in trust set"])\n\n    try:\n        signature = base64.b64decode(seal["signature"])\n    except Exception:\n        return _result(2, fmt, domain, version, ["signature invalid"])\n\n    si = pae.signing_input(cid, type_str=fmt)\n    if not any(ed25519_verify.verify(k, si, signature) for k in candidates):\n        return _result(2, fmt, domain, version, ["signature invalid"])\n\n    try:\n        expiry = _parse_iso(content["expiry"])\n    except Exception:\n        return _result(2, fmt, domain, version, ["unparseable expiry"])\n    if now > expiry:\n        return _result(2, fmt, domain, version, ["head expired"])\n\n    return _result(0, fmt, domain, version, [])\n\n\ndef _witness_payload(domain: str, version: int, merkle_root: str, prev_head) -> dict:\n    return {"domain": domain, "version": version, "merkle_root": merkle_root, "prev_head": prev_head}\n\n\ndef witness_statement_id(domain: str, version: int, merkle_root: str, prev_head) -> str:\n    """content_id of the narrow (domain, version, merkle_root, prev_head)\n    tuple a witness actually cosigns (spec Sec 6) -- deliberately NOT the\n    full head content_id, so witness software stays forward-compatible\n    with head fields it never needs to parse."""\n    return canon.content_id(_witness_payload(domain, version, merkle_root, prev_head))\n\n\ndef verify_witness_cosign(\n    cosign: dict, *, domain: str, version: int, merkle_root: str, prev_head, trust_set: set,\n) -> bool:\n    """Verify one witness cosignature ({"key_fingerprint", "signature"})\n    over PAE(\'sava-witness/1\', witness_statement_id(...)). Never\n    raises."""\n    try:\n        sid = witness_statement_id(domain, version, merkle_root, prev_head)\n        si = pae.signing_input(sid, type_str="sava-witness/1")\n        candidates = [\n            k for k in trust_set if hashlib.sha256(k).hexdigest()[:16] == cosign.get("key_fingerprint")\n        ]\n        if not candidates:\n            return False\n        signature = base64.b64decode(cosign["signature"])\n        return any(ed25519_verify.verify(k, si, signature) for k in candidates)\n    except Exception:\n        return False\n\n\ndef verify_witness_quorum(head: dict, *, witness_trust_set: set, k: int) -> int:\n    """Count DISTINCT valid witness cosigners on `head` (from\n    head[\'witnesses\']) against witness_trust_set. Returns the count --\n    the caller compares it against its own required k (spec Sec 6: a\n    client-side policy check; this function only does the crypto)."""\n    content = head["content"]\n    domain, version = content["domain"], content["version"]\n    merkle_root, prev_head = content["merkle_root"], content["prev_head"]\n    valid_fingerprints = set()\n    for cosign in head.get("witnesses", []) or []:\n        if verify_witness_cosign(\n            cosign, domain=domain, version=version, merkle_root=merkle_root,\n            prev_head=prev_head, trust_set=witness_trust_set,\n        ):\n            valid_fingerprints.add(cosign.get("key_fingerprint"))\n    return len(valid_fingerprints)\n\n\ndef verify_rotation(content_id_hex: str, rotation: dict, prior_public_key: bytes) -> bool:\n    """Verify a rotation sidecar\'s prior_key_signature: the PRIOR key\'s\n    Ed25519 signature over PAE(\'sava-key-rotation/1\', content_id) of\n    the NEW head (spec Sec 7). Confirms prior_public_key\'s own\n    fingerprint matches rotation[\'rotates_from\'] first -- a caller must\n    supply the specific old key it has pinned, not any key. Never\n    raises."""\n    try:\n        if hashlib.sha256(prior_public_key).hexdigest()[:16] != rotation.get("rotates_from"):\n            return False\n        signature = base64.b64decode(rotation["prior_key_signature"])\n        si = pae.signing_input(content_id_hex, type_str="sava-key-rotation/1")\n        return ed25519_verify.verify(prior_public_key, si, signature)\n    except Exception:\n        return False\n'),
    ('acsa.sava.rederive', '"""Source-independent re-derivation of a SAVA Drop\'s verdict.\n\nA SAVA consumer must be able to RE-DERIVE a Drop\'s verdict from\n(claim, quotes) ALONE -- no source document -- so they trust the math, not\nthe publisher. This module reproduces the source-independent branches of\nthe engine\'s `acsa.audit.recheck._derive_verdict` decision order, MINUS the\nsource-dependent grounding step (quote-in-source verification), which is\nTask 6\'s responsibility, not this module\'s.\n\nstdlib-only, Python 3.10-compatible, no pydantic / acsa.core.models /\nacsa.verify.* imports -- this module is designed to be lifted into a\nhash-pinned, dependency-free reference verifier (Task 10). Verdicts and\nreason codes are bare string literals, not enum members, so this module\nnever needs to import the engine\'s models.\n"""\nimport datetime as dt\n\nfrom acsa.sava._atoms import atom_dates, check_consistency\n\n\ndef rederive(\n    claim_text: str,\n    declared_type: str,\n    quotes: list[str],\n    graded_at: dt.datetime,\n    has_evidence: bool,\n) -> tuple[str, str | None]:\n    """Re-derive (status, reason_code) from claim text + declared type +\n    quotes + grading time alone.\n\n    Decision order (mirrors acsa.audit.recheck._derive_verdict, minus the\n    grounding step):\n      1. declared_type in {forward_looking, opinion} -> (not_established, by_nature)\n      2. declared_type == external -> (not_established, source_unavailable)\n      3. no evidence declared -> (not_established, no_evidence_declared)\n      4. a date atom in the claim text is in the future relative to\n         graded_at -> (not_established, type_declaration_suspect)\n      5. [grounding is skipped here -- source-dependent, Task 6]\n      6. atom-consistency of (claim, quotes):\n         consistent -> (verified, None)\n         contradiction -> (false, None)\n         ambiguous -> (not_established, atom_ambiguous)\n\n    `declared_type` is a bare string, not a validated enum: a properly\n    signed Drop only ever carries one of the four known values (checked\n    above), but nothing here enforces that. Any other string (typos,\n    tampered input, a future declared type) falls through branches 1-2\n    and lands on the source_checkable/consistency path (branches 3-6) by\n    design -- this function does not itself validate `declared_type`,\n    that\'s the caller\'s/Drop schema\'s job.\n    """\n    if declared_type in ("forward_looking", "opinion"):\n        return "not_established", "by_nature"\n\n    if declared_type == "external":\n        return "not_established", "source_unavailable"\n\n    if not has_evidence:\n        return "not_established", "no_evidence_declared"\n\n    today = (graded_at.year, graded_at.month, graded_at.day)\n    for y, m, d in atom_dates(claim_text):\n        if (y, m or 1, d or 1) > today:\n            return "not_established", "type_declaration_suspect"\n\n    result, _detail = check_consistency(claim_text, quotes)\n    if result == "contradiction":\n        return "false", None\n    if result == "ambiguous":\n        return "not_established", "atom_ambiguous"\n\n    return "verified", None\n'),
    ('acsa.sava.source_fidelity', '"""\nSource-fidelity verification for SAVA Drops.\nByte-exact matching (NOT normalized) of a quoted byte span against a source document.\n"""\nimport hashlib\n\n\ndef check_source_fidelity(\n    quote_bytes: bytes,\n    locator: tuple[int, int],\n    source_bytes: bytes,\n    source_sha256: str,\n) -> str:\n    """\n    Verify that a source document is genuine and a quote is byte-exact at the locator.\n\n    Args:\n        quote_bytes: The quoted bytes (from base64-decoded quote_b64).\n        locator: Tuple (start, end) of byte offsets in source_bytes.\n        source_bytes: The full source document bytes.\n        source_sha256: Expected sha256 hex digest of source_bytes.\n\n    Returns:\n        \'checked-ok\' if the source is genuine and quote is byte-exact at locator,\n        \'mismatch\' otherwise. NEVER raises.\n\n    Logic:\n      - if hashlib.sha256(source_bytes).hexdigest() != source_sha256  -> \'mismatch\'\n      - let (start, end) = locator; if not (0 <= start < end <= len(source_bytes)) -> \'mismatch\'\n      - if source_bytes[start:end] != quote_bytes -> \'mismatch\'\n      - else \'checked-ok\'\n    """\n    try:\n        # Step 1: Verify source authenticity\n        actual_sha256 = hashlib.sha256(source_bytes).hexdigest()\n        if actual_sha256 != source_sha256:\n            return "mismatch"\n\n        # Step 2: Validate locator range\n        start, end = locator\n        if not (0 <= start < end <= len(source_bytes)):\n            return "mismatch"\n\n        # Step 3: Byte-exact comparison\n        actual_quote = source_bytes[start:end]\n        if actual_quote != quote_bytes:\n            return "mismatch"\n\n        # All checks passed\n        return "checked-ok"\n\n    except Exception:\n        # Never raise; wrap everything in try/except\n        return "mismatch"\n'),
    ('acsa.sava.verify', '"""SAVA Drop verifier -- the RECEIVER side of sava-drop/1 (spec sec 8).\n\nverify_drop() is the consumer-side counterpart to acsa.sava.produce\'s\nproduce_drop(): given the raw wire bytes of a Drop and a trust_set of\npublic keys, it establishes -- WITHOUT trusting anything the wire itself\nclaims -- whether the Drop\'s content address, signer authenticity, and\n(for proof-carrying Drops) re-derived verdict all agree.\n\nPortability (Task 10): this module is lifted VERBATIM into the portable,\nhash-pinned, dependency-free reference verifier, so it MUST stay\nstdlib-only at module level (hashlib, base64, datetime -- json parsing\nitself lives in canon.strict_decode) and Python-3.10-compatible. It\nimports ONLY its sava sibling modules (canon, pae, ed25519_verify,\nrederive, source_fidelity) -- never pydantic, acsa.core.models,\nacsa.verify.*, or acsa.crypto (which pulls in pynacl at module level).\nThe key fingerprint algorithm is reproduced here with plain hashlib\n(matching acsa.crypto.fingerprint byte-for-byte) rather than imported,\nfor exactly that reason. The strict-decode guards (duplicate keys,\nNaN/Infinity, trailing data) live once in canon.strict_decode and are\nreused here, rather than re-implemented, so root-of-trust parsing logic\nnever has two copies to drift apart before Task 10 pins one of them.\n\nCOMPOSITION (Task 9, spec Sec 10): a derived Drop\'s content.derived_from\nembeds full input Drops (content+seal each). Verifying one recursively\nverifies every embedded input, then folds the results two ways:\n  - composer-honesty: the weakest of the inputs\' own SEALED verdicts\n    (ignoring trust -- what the composer actually saw) must equal the\n    derived Drop\'s own sealed verdict. A mismatch means the composer\n    sealed a verdict its own inputs don\'t support -- result 2.\n  - weakest-trusted-signer: the weakest of the inputs\' TRUST-ADJUSTED\n    verdicts (an input whose signer isn\'t in trust_set can\'t be trusted,\n    so it folds in as "not_established" regardless of what it claims) may\n    DIFFER from the composer\'s sealed verdict -- in EITHER direction, not\n    just weaker. An untrusted "false" input, for instance, gets clamped\n    UP to "not_established" for the fold (we can\'t confirm it\'s actually\n    false, only that we can\'t vouch for it), so a composite honestly\n    sealed "false" on the strength of that one untrusted input degrades\n    to "not_established", not the numerically-"stronger" clamp value.\n    Any mismatch between the trust-adjusted verdict and the sealed one is\n    a legitimate receiver-side degradation -- what THIS receiver, with\n    THIS trust_set, can actually stand behind -- so it\'s still result 0,\n    just at the trust-adjusted verdict, never result 2.\nA cyclic derived_from (a node embedding itself, directly or transitively)\nis detectable only by Python object identity, never by content_id: since\ncontent_id requires fully serializing every embedded input, a node can\nnever truly contain itself and still be finitely hashable (the standard\nMerkle-DAG acyclicity property) -- a live reference cycle can only arise\nfrom a hand-forged in-memory structure, never from valid JSON bytes\nproduced by an honest signer. That does NOT mean canonicalizing such a\nstructure would fail safely on its own: canon.canonicalize goes through\nthe vendored, hash-pinned jcs.dump (RFC 8785), a plain recursive\nserializer with NO cycle detection of its own -- unlike stdlib\njson.dumps, it would recurse without bound (eventually a RecursionError)\non a live circular reference. _check_structure() is therefore the ONLY\nguard against that: it walks the structure by Python object identity\nBEFORE any canonicalization is attempted, so a cyclic (or excessively\ndeep) derived_from is rejected cheaply and safely. Do not assume\ncanonicalize()/jcs.dump is cycle-safe on its own -- it is not; this\nguard is load-bearing and must keep running first.\n\nTRUST MODEL: the wire Seal carries only a 16-hex-char key_fingerprint,\nnever the public key itself. The caller supplies trust_set, a set of raw\n32-byte Ed25519 public keys it trusts. This module computes\nsha256(key).hexdigest()[:16] for every key in trust_set and collects ALL\nwhose fingerprint matches seal["key_fingerprint"] -- normally exactly\none, but a growing/rotating trust_set could in principle contain more\nthan one 16-hex-char fingerprint collision, astronomically unlikely as\nthat is for a root-of-trust check. No fingerprint match at all means\nauthenticity cannot be established for this signer (result 3). If at\nleast one candidate matches by fingerprint, the signature is checked\nagainst EVERY candidate in turn; only if none of them actually verifies\ndoes this degrade to result 2 ("signature invalid") -- a single\nfingerprint match that happens to be the wrong key never masks a\ndifferent, genuinely valid signer also present in trust_set.\n\nRESULT CODES:\n  0 -- Drop confirmed: content-address integrity, signer authenticity,\n       and (for proof-carrying Drops) verdict re-derivation all check\n       out. Independent of WHICH verdict was sealed -- a correctly\n       verified "false" Drop is still result 0.\n  1 -- operational: a hard parse/format failure or an unexpected internal\n       error prevented any decision from being reached.\n  2 -- failed: the Drop parsed and its signer is trusted, but something\n       about the Drop itself is wrong: invalid signature, a re-derived\n       verdict disagreeing with the sealed one, a supplied source document\n       disagreeing with a quote, a cyclic/too-deep derived_from, an\n       embedded input that itself fails to verify, or a composer-sealed\n       verdict that doesn\'t match its own inputs\' weakest.\n  3 -- authenticity not established: seal["key_fingerprint"] matches no\n       key in trust_set.\n"""\nimport base64\nimport datetime as dt\nimport hashlib\n\nfrom acsa.sava import canon, ed25519_verify, pae\nfrom acsa.sava.rederive import rederive\nfrom acsa.sava.source_fidelity import check_source_fidelity\n\n\ndef _result(result: int, source_fidelity: str, verdict, reasons: list) -> dict:\n    return {\n        "result": result,\n        "source_fidelity": source_fidelity,\n        "verdict": verdict,\n        "reasons": reasons,\n    }\n\n\ndef _parse_iso(s: str) -> dt.datetime:\n    """Parse an ISO-8601 datetime string, tolerating a trailing \'Z\'.\n\n    Python\'s datetime.fromisoformat() only started accepting a bare\n    trailing \'Z\' in 3.11; this module must behave identically back to\n    3.10 (it is lifted verbatim into the Task 10 reference verifier), and\n    pydantic\'s model_dump(mode="json") -- what produce_drop\'s Content\n    serializes through -- emits exactly that trailing-\'Z\' form for\n    UTC-aware datetimes. Normalizing \'Z\' -> \'+00:00\' first keeps\n    fromisoformat\'s parsing identical on 3.10 and on later interpreters.\n    """\n    if s.endswith("Z"):\n        s = s[:-1] + "+00:00"\n    return dt.datetime.fromisoformat(s)\n\n\ndef verify_drop(\n    raw: bytes, *, sources: dict[str, bytes] | None = None, trust_set: set[bytes]\n) -> dict:\n    """Verify a sava-drop/1 Drop (raw JSON bytes).\n\n    Args:\n        raw: The raw wire bytes of a Drop: {"content": {...}, "seal": {...}}.\n        sources: optional source_id -> full source document bytes, for\n            checking included evidence entries\' quotes byte-exactly\n            against the actual source. Entries whose source_id is absent\n            from `sources` (or when `sources` is None) are simply not\n            fidelity-checked; this never by itself fails verification.\n            Coverage is all-or-nothing: if a proof-carrying Drop has 2+\n            included entries and only SOME of their sources are supplied,\n            the result is `source_fidelity="not-present"`, not a partial\n            "checked-ok" -- an incomplete check is deliberately treated\n            the same as no check at all, never reported as stronger than\n            it actually is.\n        trust_set: set of raw 32-byte Ed25519 public keys the receiver\n            trusts. The signer is identified by matching\n            sha256(key).hexdigest()[:16] against the seal\'s\n            key_fingerprint.\n\n    Returns:\n        {"result": 0|1|2|3,\n         "source_fidelity": "checked-ok"|"not-present"|"mismatch",\n         "verdict": str|None,\n         "reasons": [str, ...]}\n\n    Never raises: any unexpected internal error is caught and reported as\n    result 1 ("operational").\n    """\n    try:\n        return _verify_drop(raw, sources=sources, trust_set=trust_set)\n    except Exception as e:\n        return _result(1, "not-present", None, [f"internal error: {e}"])\n\n\ndef _verify_drop(raw: bytes, *, sources, trust_set) -> dict:\n    # Step 1: strict-parse. A hard parse/format failure that prevents any\n    # decision -> result 1.\n    try:\n        drop = canon.strict_decode(raw)\n    except Exception as e:\n        return _result(1, "not-present", None, [f"parse error: {e}"])\n\n    return _verify_obj(drop, sources=sources, trust_set=trust_set)\n\n\n#: Composition weakest-verdict ordering (spec Sec 10), reproduced here\n#: (not imported from acsa.sava.produce) so this module stays\n#: stdlib-only/portable: false is weakest, verified is strongest.\n_WEAKEST_ORDER = {"false": 0, "not_established": 1, "verified": 2}\n\n#: Hard cap on derived_from nesting depth. Defends against both a\n#: pathologically deep (but genuinely acyclic) chain and -- together with\n#: _check_structure\'s identity-based cycle check -- a hand-forged, live\n#: circular in-memory structure that could otherwise make canon.content_id\n#: recurse without bound (see module docstring: COMPOSITION).\n_MAX_DERIVED_DEPTH = 32\n\n\ndef _check_structure(content, _seen_ids: frozenset, _depth: int) -> str | None:\n    """Walk content[\'derived_from\'] purely by Python object identity,\n    NEVER by content_id/hashing -- so this is safe to run even on a\n    hand-forged structure containing a live reference cycle (canon.content_id\n    would recurse without bound on such a structure; this walk detects the\n    repeat on first re-visit and never attempts to canonicalize it).\n\n    Returns an error reason string ("cyclic derived_from" or "max\n    derived_from depth exceeded") if the reachable structure is unsafe to\n    recurse into, else None.\n    """\n    if not isinstance(content, dict):\n        return None\n    if id(content) in _seen_ids:\n        return "cyclic derived_from"\n    if _depth > _MAX_DERIVED_DEPTH:\n        return "max derived_from depth exceeded"\n\n    derived = content.get("derived_from")\n    if not derived or not isinstance(derived, (list, tuple)):\n        return None\n\n    seen_ids = _seen_ids | {id(content)}\n    for child in derived:\n        if not isinstance(child, dict):\n            continue\n        err = _check_structure(child.get("content"), seen_ids, _depth + 1)\n        if err:\n            return err\n    return None\n\n\ndef _verify_obj(drop, *, sources, trust_set) -> dict:\n    """Verify an already-parsed Drop object {"content": ..., "seal": ...}.\n\n    This is the internal counterpart to verify_drop() that operates on a\n    Python dict directly rather than raw bytes -- used both by verify_drop\n    itself (after strict-decoding the wire bytes) and recursively by\n    _verify_content for each embedded input. It is also the one entry\n    point that can be exercised directly on a hand-forged, live circular\n    structure -- something no honest signer can produce (see module\n    docstring: COMPOSITION), and not expressible as raw JSON bytes either\n    (stdlib json.dumps refuses to serialize a circular reference, so a\n    caller can\'t even get `raw` bytes for one). That does NOT make this\n    function safe to skip the guard for: the canonicalizer this module\n    actually uses (canon.canonicalize -> the vendored jcs.dump) has NO\n    cycle detection of its own and would recurse without bound on such a\n    structure if handed one directly. _check_structure() runs FIRST, here,\n    for exactly that reason -- it is the only thing standing between a\n    hand-forged circular structure and unbounded recursion.\n    """\n    if not isinstance(drop, dict) or "content" not in drop or "seal" not in drop:\n        return _result(1, "not-present", None, ["malformed drop: missing content or seal"])\n\n    content = drop["content"]\n    seal = drop["seal"]\n    if not isinstance(content, dict) or not isinstance(seal, dict):\n        return _result(1, "not-present", None, ["malformed drop: content/seal must be objects"])\n\n    struct_err = _check_structure(content, frozenset(), 0)\n    if struct_err:\n        return _result(2, "not-present", content.get("verdict"), [struct_err])\n\n    return _verify_content(content, seal, sources=sources, trust_set=trust_set, _depth=0)\n\n\ndef _verify_content(content: dict, seal: dict, *, sources, trust_set, _depth: int) -> dict:\n    verdict_wire = content.get("verdict")\n\n    # Step 2: content address. Recomputed from the parsed `content`\n    # sub-object -- canon.content_id canonicalizes it internally -- NEVER\n    # trust any wire-supplied content_id (there isn\'t even a field for\n    # one: the address is always derived, never carried). Safe to call\n    # unconditionally here: _verify_obj\'s _check_structure call already\n    # proved the FULL reachable tree (this node and everything under it)\n    # is finite and acyclic before _verify_content was ever invoked, for\n    # both the outermost call and every recursive one below.\n    cid = canon.content_id(content)\n\n    # Step 3: authenticity. Find EVERY trusted key whose fingerprint\n    # matches the seal\'s -- the seal itself never carries the public key,\n    # only the fingerprint, so a growing/rotating trust_set could in\n    # principle contain more than one 16-hex-char match.\n    key_fingerprint = seal.get("key_fingerprint")\n    candidates = [\n        key for key in trust_set if hashlib.sha256(key).hexdigest()[:16] == key_fingerprint\n    ]\n\n    if not candidates:\n        return _result(3, "not-present", verdict_wire, ["signer not in trust set"])\n\n    try:\n        signature = base64.b64decode(seal["signature"])\n    except Exception:\n        return _result(2, "not-present", verdict_wire, ["signature invalid"])\n\n    # Try EVERY fingerprint-matching candidate, not just the first: a\n    # single colliding-but-wrong key must never mask a different,\n    # genuinely valid signer also present in trust_set.\n    si = pae.signing_input(cid)\n    if not any(ed25519_verify.verify(key, si, signature) for key in candidates):\n        return _result(2, "not-present", verdict_wire, ["signature invalid"])\n\n    # Step 4: proof. Collect ALL included evidence quotes into one list\n    # and re-derive the verdict ONCE, source-independently. Content has\n    # no declared_type field: a proof-carrying Drop (>=1 included entry)\n    # is by definition source_checkable-with-evidence by construction, so\n    # that\'s what we re-derive against. An attestation Drop (no included\n    # entries) has nothing to re-derive -- the step is vacuous.\n    evidence = content.get("evidence") or []\n    included = [e for e in evidence if isinstance(e, dict) and e.get("source_status") == "included"]\n\n    if included:\n        quotes = [base64.b64decode(e["quote_b64"]).decode("utf-8") for e in included]\n        graded_at = _parse_iso(content["graded_at"])\n        status, reason_code = rederive(content["claim"], "source_checkable", quotes, graded_at, True)\n        sealed = (content.get("verdict"), content.get("reason_code"))\n        if (status, reason_code) != sealed:\n            return _result(2, "not-present", verdict_wire, ["verdict re-derivation mismatch"])\n\n        checked = 0\n        for e in included:\n            source_id = e.get("source_id")\n            if sources is not None and source_id in sources:\n                fidelity = check_source_fidelity(\n                    base64.b64decode(e["quote_b64"]),\n                    tuple(e["locator"]),\n                    sources[source_id],\n                    e.get("source_sha256"),\n                )\n                if fidelity == "mismatch":\n                    return _result(2, "mismatch", verdict_wire, ["source fidelity mismatch"])\n                checked += 1\n        source_fidelity = "checked-ok" if checked and checked == len(included) else "not-present"\n    else:\n        source_fidelity = "not-present"\n\n    # Step 5: composition (spec Sec 10). A derived Drop embeds its full\n    # inputs in content["derived_from"]; recursively verify EACH one, then\n    # fold the results two distinct ways -- see module docstring.\n    derived_from = content.get("derived_from") or []\n    if derived_from:\n        if not isinstance(derived_from, (list, tuple)):\n            return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n\n        embedded_verdicts = []  # inputs\' own SEALED verdicts, ignoring trust\n        trusted_verdicts = []  # inputs\' TRUST-ADJUSTED verdicts\n\n        for child in derived_from:\n            if not isinstance(child, dict):\n                return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n            child_content = child.get("content")\n            child_seal = child.get("seal")\n            if not isinstance(child_content, dict) or not isinstance(child_seal, dict):\n                return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n            child_sealed_verdict = child_content.get("verdict")\n            if child_sealed_verdict not in _WEAKEST_ORDER:\n                return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n            embedded_verdicts.append(child_sealed_verdict)\n\n            try:\n                child_result = _verify_content(\n                    child_content, child_seal, sources=sources, trust_set=trust_set,\n                    _depth=_depth + 1,\n                )\n            except Exception:\n                return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n\n            if child_result["result"] == 0:\n                # Trusted: fold in the input\'s OWN (possibly already\n                # trust-degraded) verdict.\n                trusted_verdicts.append(child_result["verdict"])\n            elif child_result["result"] == 3:\n                # Not trusted: can\'t vouch for this input\'s verdict at\n                # all -- neither confirm it nor confirm it\'s false -- so\n                # for the trust fold it\'s clamped to "not_established"\n                # regardless of what it claims (even a "false" verdict,\n                # which is numerically WEAKER than not_established under\n                # _WEAKEST_ORDER, gets clamped UP: an unconfirmed "false"\n                # is not the same as a confirmed one). Note this also\n                # means the untrusted child\'s OWN signature was never\n                # even checked -- result 3 fires on fingerprint mismatch\n                # alone, before _verify_content ever looks at the\n                # signature bytes, so this path is safe even against a\n                # child carrying a garbage/forged signature.\n                trusted_verdicts.append("not_established")\n            else:\n                # result 1 or 2: tampered, invalid, or itself a bad\n                # composition -- poisons the whole composite.\n                return _result(2, "not-present", verdict_wire, ["invalid input drop"])\n\n        # Composer-honesty: what the composer actually saw (the inputs\'\n        # own sealed verdicts, ignoring trust) must reproduce the\n        # composite\'s own sealed verdict. A mismatch means the composer\n        # sealed a verdict its own inputs don\'t support.\n        weakest_all = min(embedded_verdicts, key=_WEAKEST_ORDER.__getitem__)\n        if weakest_all != content.get("verdict"):\n            return _result(2, "not-present", verdict_wire, ["composition verdict mismatch"])\n\n        # Weakest-trusted-signer: what THIS receiver, with THIS trust_set,\n        # can actually stand behind. This MUST be a plain !=, not "is\n        # effective weaker than sealed" -- an untrusted input clamps to\n        # "not_established" (rank 1) regardless of its own claimed\n        # verdict, and when the sealed derived verdict is "false" (rank\n        # 0, the floor of _WEAKEST_ORDER) an untrusted contributor can\n        # make effective LAND ABOVE the sealed rank (not_established,\n        # rank 1 > false, rank 0) without ever being "weaker" by that\n        # comparison -- a composite honestly sealed "false" purely on the\n        # strength of one untrusted signer would then be reported as\n        # fully confirmed "false" with no caveats. Any mismatch, in\n        # either direction, means the sealed verdict isn\'t what this\n        # trust_set can independently confirm -- a legitimate\n        # receiver-side degradation, not a composer lie, so it\'s still\n        # result 0, just at the trust-adjusted verdict.\n        effective = min(trusted_verdicts, key=_WEAKEST_ORDER.__getitem__)\n        if effective != content.get("verdict"):\n            return _result(\n                0,\n                "not-present",\n                effective,\n                [f"degraded to {effective}: weakest-trusted-signer"],\n            )\n\n        return _result(0, "not-present", content.get("verdict"), [])\n\n    # Step 6: result. Integrity + authenticity + (for proof-carrying)\n    # re-derivation all passed.\n    return _result(0, source_fidelity, verdict_wire, [])\n'),
]

#: The three synthetic "package" modules the embedded modules' own
#: `from acsa.sava... import ...` statements are written against. Each
#: is a bare types.ModuleType standing in for the real package -- never
#: backed by any file on disk -- so this script never touches sys.path
#: and can run from anywhere, with nothing importable named "acsa" on the
#: host at all.
_PACKAGE_NAMES = ("acsa", "acsa.sava", "acsa.sava._vendored")


def _register(name: str, mod: "types.ModuleType") -> None:
    """Register `mod` in sys.modules under `name`, and -- if `name` is
    dotted -- also set it as an attribute of its already-registered parent
    package module. The attribute step is what makes `from acsa.sava
    import canon` (a "from package import submodule" form) resolve
    correctly: Python's import system only auto-imports a missing
    fromlist submodule when the parent lacks that attribute, and this
    script deliberately never lets the real import machinery touch the
    filesystem, so the attribute must be set explicitly here instead.
    """
    sys.modules[name] = mod
    if "." in name:
        parent_name, attr = name.rsplit(".", 1)
        parent = sys.modules.get(parent_name)
        if parent is not None:
            setattr(parent, attr, mod)


def _load_modules() -> "types.ModuleType":
    """Exec every embedded module source into its own namespace, in
    dependency order, registered under its ORIGINAL dotted name -- so each
    module's own unmodified `from acsa.sava... import ...` statements
    resolve exactly as they do in the real package. Returns the fully
    loaded acsa.sava.verify module.
    """
    for name in _PACKAGE_NAMES:
        pkg = types.ModuleType(name)
        pkg.__path__ = []  # marks it as a package to the import system
        _register(name, pkg)

    # acsa.sava._vendored.ed25519_pure runs a small self-test (an RFC
    # 8032 §7.1 test vector) at module-import time and prints "All tests
    # passed." on success. That self-test is a genuine safety net -- if
    # the pure-Python implementation is ever broken on the host
    # interpreter, exec()'ing it below raises immediately instead of
    # silently shipping a broken verifier -- so it keeps running exactly
    # as it does in the real package. Only its stdout is swallowed here,
    # so a --json caller's stdout is nothing but the JSON result; no
    # module behavior changes.
    with contextlib.redirect_stdout(io.StringIO()):
        for name, src in _MODULES:
            mod = types.ModuleType(name)
            mod.__file__ = f"<sava-embedded:{name}>"
            _register(name, mod)
            exec(compile(src, mod.__file__, "exec"), mod.__dict__)

    return sys.modules["acsa.sava.verify"]


_verify_mod = _load_modules()
verify_drop = _verify_mod.verify_drop
_head_mod = sys.modules["acsa.sava.head_verify"]
verify_head = _head_mod.verify_head
_merkle_mod = sys.modules["acsa.sava.merkle"]
verify_inclusion = _merkle_mod.verify_inclusion
verify_non_inclusion = _merkle_mod.verify_non_inclusion


def _parse_iso_cli(s: str) -> "dt.datetime":
    if s.endswith("Z"):
        s = s[:-1] + "+00:00"
    return dt.datetime.fromisoformat(s)


class _UsageError(Exception):
    """Raised for ANY CLI-usage-level problem -- a bad flag, a missing
    positional, or a bad --trust/--sources value -- instead of ever calling
    sys.exit(2). argparse.ArgumentParser.error() normally exits 2 directly,
    which would be indistinguishable from this script's OWN "result 2 =
    verification failed" -- and would contradict this file's own docstring
    (Exit code == result["result"]; usage problems are always 1,
    "operational", never 2). See _ArgumentParser below and this codebase's
    existing acsa.serve.cli._UsageError/_ACArgumentParser, which solves the
    identical problem the identical way.
    """


class _ArgumentParser(argparse.ArgumentParser):
    """ArgumentParser that raises _UsageError instead of calling
    sys.exit(2) on a usage error (see _UsageError). Does NOT touch
    parse_args' other exit path (-h/--help still exits 0 as normal --
    only error() is overridden)."""

    def error(self, message):
        raise _UsageError(message)


def _looks_like_hex_pubkey(s: str) -> bool:
    return len(s) == 64 and all(c in "0123456789abcdefABCDEF" for c in s)


def _load_trust_key(value: str) -> bytes:
    """Resolve one --trust argument to a raw 32-byte Ed25519 public key.

    Accepts, in order: a bare 64-character hex string; or a path to a file
    containing the key as raw 32 bytes, as 64 hex characters (optionally
    with surrounding whitespace), or as base64 text.
    """
    s = value.strip()
    if _looks_like_hex_pubkey(s):
        return bytes.fromhex(s)

    path = Path(value)
    if not path.is_file():
        raise _UsageError(
            f"--trust: {value!r} is neither a 64-character hex Ed25519 "
            f"public key nor an existing file"
        )
    try:
        data = path.read_bytes()
    except OSError as e:
        raise _UsageError(f"--trust: cannot read {value!r}: {e}") from e

    if len(data) == 32:
        return data

    text = data.decode("utf-8", errors="ignore").strip()
    if _looks_like_hex_pubkey(text):
        return bytes.fromhex(text)

    try:
        decoded = base64.b64decode(text, validate=True)
    except Exception:
        decoded = b""
    if len(decoded) == 32:
        return decoded

    raise _UsageError(
        f"--trust: {value!r} does not contain a recognizable 32-byte "
        f"Ed25519 public key (tried raw bytes, hex, base64)"
    )


def _read_sources(sources_dir: "str | None") -> "dict[str, bytes] | None":
    if sources_dir is None:
        return None
    base = Path(sources_dir)
    if not base.is_dir():
        raise _UsageError(f"--sources: not a directory: {sources_dir}")
    out: "dict[str, bytes]" = {}
    for p in sorted(base.iterdir()):
        if p.is_file():
            try:
                out[p.name] = p.read_bytes()
            except OSError as e:
                raise _UsageError(f"--sources: cannot read {p}: {e}") from e
    return out


_RESULT_LABELS = {
    0: "CONFIRMED",
    1: "OPERATIONAL ERROR",
    2: "FAILED",
    3: "AUTHENTICITY NOT ESTABLISHED",
}


def _human_readable(result: dict) -> str:
    label = _RESULT_LABELS.get(result["result"], "UNKNOWN")
    lines = [
        f"sava-verify: {label} (result={result['result']})",
        f"  verdict:          {result['verdict']}",
        f"  source_fidelity:  {result['source_fidelity']}",
    ]
    if result["reasons"]:
        lines.append("  reasons:")
        for r in result["reasons"]:
            lines.append(f"    - {r}")
    return "\n".join(lines)


def _drop_mode(args) -> int:
    raw = Path(args.path).read_bytes()
    sources = _read_sources(args.sources)
    trust_set = {_load_trust_key(v) for v in args.trust}
    result = verify_drop(raw, sources=sources, trust_set=trust_set)
    print(json.dumps(result) if args.json else _human_readable(result))
    return int(result["result"])


def _head_mode(args) -> int:
    raw = Path(args.path).read_bytes()
    trust_set = {_load_trust_key(v) for v in args.trust}
    now = _parse_iso_cli(args.now)
    result = verify_head(raw, trust_set=trust_set, now=now)
    print(json.dumps(result) if args.json else json.dumps(result, indent=2))
    return int(result["result"])


def _inclusion_mode(args) -> int:
    proof = json.loads(Path(args.path).read_bytes())
    ok = verify_inclusion(proof, args.root)
    print(json.dumps({"valid": ok}) if args.json else ("valid" if ok else "invalid"))
    return 0 if ok else 2


def _non_inclusion_mode(args) -> int:
    proof = json.loads(Path(args.path).read_bytes())
    ok = verify_non_inclusion(proof, args.root)
    print(json.dumps({"valid": ok}) if args.json else ("valid" if ok else "invalid"))
    return 0 if ok else 2


def main(argv=None) -> int:
    parser = _ArgumentParser(
        prog="sava_verify.py",
        description="Self-contained, stdlib-only reference verifier for SAVA objects.",
    )
    sub = parser.add_subparsers(dest="mode", required=True)

    drop_p = sub.add_parser("drop", help="Verify a sava-drop/1 Drop.")
    drop_p.add_argument("path", help="Path to a Drop JSON file.")
    drop_p.add_argument("--sources", default=None, metavar="DIR")
    drop_p.add_argument("--trust", action="append", default=[], metavar="KEY")
    drop_p.add_argument("--json", action="store_true")
    drop_p.set_defaults(func=_drop_mode)

    head_p = sub.add_parser("head", help="Verify a sava-pond/1 or sava-lake/1 collection head.")
    head_p.add_argument("path", help="Path to a collection-head JSON file.")
    head_p.add_argument("--trust", action="append", default=[], metavar="KEY")
    head_p.add_argument("--now", required=True, metavar="ISO8601")
    head_p.add_argument("--json", action="store_true")
    head_p.set_defaults(func=_head_mode)

    inclusion_p = sub.add_parser("inclusion", help="Verify a Merkle inclusion proof.")
    inclusion_p.add_argument("path", help="Path to an inclusion-proof JSON file.")
    inclusion_p.add_argument("--root", required=True, metavar="HEX")
    inclusion_p.add_argument("--json", action="store_true")
    inclusion_p.set_defaults(func=_inclusion_mode)

    non_inclusion_p = sub.add_parser("non-inclusion", help="Verify a Merkle non-inclusion proof.")
    non_inclusion_p.add_argument("path", help="Path to a non-inclusion-proof JSON file.")
    non_inclusion_p.add_argument("--root", required=True, metavar="HEX")
    non_inclusion_p.add_argument("--json", action="store_true")
    non_inclusion_p.set_defaults(func=_non_inclusion_mode)

    try:
        args = parser.parse_args(argv)
        return args.func(args)
    except _UsageError as e:
        print(f"error: {e}", file=sys.stderr)
        return 1
    except OSError as e:
        print(f"error: {e}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    sys.exit(main())
